import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  root: {
    height: "100%",
    marginTop: 30,
  },
  textTitle: {
    textAlign: "center",
  },
  SearchContainer: {
    marginTop: 20,
    marginBottom: 10,
  },
  ml: {
    marginLeft: 5,
  },
  mb: {
    marginBottom: 10,
  },
});

export default useStyles;
